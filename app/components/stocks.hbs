<StockCalculator
  @leverage={{this.leverage}}
  @size={{this.size}}
  @margin={{this.margin}}
  @entryPrice={{this.entryPrice}}
  @marketPrice={{this.marketPrice}}
  @distributionNumber={{this.distributionNumber}}
  @liquidationPrice={{this.liquidationPrice}}
/>

<LiquidationCalculator
  @leverage={{this.leverage}}
  @size={{this.size}}
  @margin={{this.margin}}
  @entryPrice={{this.entryPrice}}
  @marketPrice={{this.marketPrice}}
  @distributionNumber={{this.distributionNumber}}
  @liquidationPrice={{this.liquidationPrice}}
/>
<EntryDistribution
  @leverage={{this.leverage}}
  @size={{this.size}}
  @margin={{this.margin}}
  @entryPrice={{this.entryPrice}}
  @marketPrice={{this.marketPrice}}
  @distributionNumber={{this.distributionNumber}}
  @liquidationPrice={{this.liquidationPrice}}
/>